zig-out/
.zig-cache/
zig-cache/
# Where the package manager unpacks the fetched codec sources (build/codecs.zig)
zig-pkg/
test/spec/conformance-failures.txt
specs/

# Example app build output
examples/macos/chartview

# Where scripts/package-release.sh builds and stages a release
out/
dist/
